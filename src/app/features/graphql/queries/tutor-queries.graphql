query getTutor($name: String!) {
  Tutor(name: $name) {
    name
    teachesLanguage {
      _id
      name
    }
  }
}

mutation saveTutor($name: String!) {
  CreateTutor(name: $name) {
    name
  }
}